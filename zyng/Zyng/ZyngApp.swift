import SwiftUI

@main
struct ZyngApp: App {

    /// Здесь НЕЛЬЗЯ делать тяжёлую работу.
    ///
    /// В сборке 45 я поставил в этот init создание настроек, списка серверов и
    /// управления туннелем — хотел по отметкам в консоли найти, на чём виснет
    /// запуск. Вышло наоборот: приложение перестало запускаться совсем.
    ///
    /// init у App вызывается ДО того, как система закончила поднимать
    /// приложение: сцены ещё нет, окна ещё нет. VPNController при создании
    /// подписывается на уведомления системы и обращается к хранилищу
    /// конфигураций VPN — в этот момент так делать нельзя.
    ///
    /// Всё это и раньше создавалось само, при первом обращении из ContentView,
    /// то есть уже на живом приложении. Так и оставляем. Одна строка в журнал
    /// ничего не стоит и подтверждает, что наш код вообще начал выполняться.
    init() {
        NSLog("🟢 Zyng: старт приложения")
    }

    var body: some Scene {
        WindowGroup {
            // Тему выбирает пользователь — ContentView применяет её сам.
            // Жёсткое .dark здесь перекрывало этот выбор.
            ContentView()
                .onAppear { NSLog("🟢 Zyng: главный экран показан") }
        }
    }
}
